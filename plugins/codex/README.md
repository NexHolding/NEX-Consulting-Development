Codex Plugin (Platzhalter)

Dieses Verzeichnis enthält die Konfigurations-Hinweise für das Codex-Plugin.

Konfiguration:
- API_KEY in `.env` speichern: `CODEX_API_KEY=...`
- Implementiere Auth-Wrapper und Beispiel-Requests in `plugins/codex/`.

Sobald Du einen API-Key bereitstellst, kann ich Beispielcode und Integration hinzufügen.
